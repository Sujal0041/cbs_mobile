import { Text, View } from 'react-native';

const Support = () => {
    return (
        <View
            style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}
        >
            <Text>Here you can view the Support</Text>
        </View>
    );
};

export default Support;
